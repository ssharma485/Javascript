'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var utils = require('../lib/utils.cjs');
var curry = require('../node_modules/.pnpm/ramda@0.30.1/node_modules/ramda/es/curry.cjs');

/**
 * exclude data if targetValue is an array
 *
 * @param {*function} filterCallback
 * @param {*object} searchCondition: { name, value }
 * @param {*boolean} caseSensitive: false
 * @param {*DataObject} data
 */
var excludeIfTargetValueIsArray = function (predicator, searchCondition, caseSensitive, data) {
    var key = searchCondition.key;
    var targetValue = utils.getObjValue(data, key);
    if (Array.isArray(targetValue)) {
        return false;
    }
    return predicator(searchCondition, caseSensitive, data);
};
var targetValueArray = curry.default(excludeIfTargetValueIsArray);

exports.default = targetValueArray;
//# sourceMappingURL=targetValueArray.cjs.map
