'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _curry1 = require('./_curry1.cjs');
var _isPlaceholder = require('./_isPlaceholder.cjs');

/**
 * Optimized internal two-arity curry function.
 *
 * @private
 * @category Function
 * @param {Function} fn The function to curry.
 * @return {Function} The curried function.
 */
function _curry2(fn) {
  return function f2(a, b) {
    switch (arguments.length) {
      case 0:
        return f2;
      case 1:
        return _isPlaceholder.default(a) ? f2 : _curry1.default(function (_b) {
          return fn(a, _b);
        });
      default:
        return _isPlaceholder.default(a) && _isPlaceholder.default(b) ? f2 : _isPlaceholder.default(a) ? _curry1.default(function (_a) {
          return fn(_a, b);
        }) : _isPlaceholder.default(b) ? _curry1.default(function (_b) {
          return fn(a, _b);
        }) : fn(a, b);
    }
  };
}

exports.default = _curry2;
//# sourceMappingURL=_curry2.cjs.map
