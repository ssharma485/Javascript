'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var _curry2 = require('./internal/_curry2.cjs');
var _dispatchable = require('./internal/_dispatchable.cjs');
var _xdrop = require('./internal/_xdrop.cjs');
var slice = require('./slice.cjs');

/**
 * Returns all but the first `n` elements of the given list, string, or
 * transducer/transformer (or object with a `drop` method).
 *
 * Dispatches to the `drop` method of the second argument, if present.
 *
 * @func
 * @memberOf R
 * @since v0.1.0
 * @category List
 * @sig Number -> [a] -> [a]
 * @sig Number -> String -> String
 * @param {Number} n
 * @param {*} list
 * @return {*} A copy of list without the first `n` elements
 * @see R.take, R.transduce, R.dropLast, R.dropWhile
 * @example
 *
 *      R.drop(1, ['foo', 'bar', 'baz']); //=> ['bar', 'baz']
 *      R.drop(2, ['foo', 'bar', 'baz']); //=> ['baz']
 *      R.drop(3, ['foo', 'bar', 'baz']); //=> []
 *      R.drop(4, ['foo', 'bar', 'baz']); //=> []
 *      R.drop(3, 'ramda');               //=> 'da'
 */
var drop = /*#__PURE__*/_curry2.default( /*#__PURE__*/_dispatchable.default(['drop'], _xdrop.default, function drop(n, xs) {
  return slice.default(Math.max(0, n), Infinity, xs);
}));
var drop$1 = drop;

exports.default = drop$1;
//# sourceMappingURL=drop.cjs.map
